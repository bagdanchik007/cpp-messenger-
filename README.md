# CppMessenger

A modern Telegram-inspired messenger built with C++.

## Architecture

- `client/` — desktop client
- `server/` — backend server
- `shared/` — shared protocol, models and utilities
- `tests/` — unit, integration and end-to-end tests
- `docs/` — architecture and development documentation

## Status

Early project setup.
